#!/usr/bin/env bash
# Run STT: Apple on-device speech recognition (eli-stt).
# Reads config.yaml, launches eli-stt with the right flags.
# Usage: stt/run
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/.venv/bin/python" -u "$DIR/launch.py" "$@"
